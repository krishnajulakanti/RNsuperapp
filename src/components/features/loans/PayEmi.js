import React from 'react';
import { View, Text, StyleSheet, FlatList, TouchableOpacity, ScrollView } from 'react-native';

const PayEmi = () => {
    return (
        <Text>Pay Emi Screen</Text>
    )
};

export default PayEmi;